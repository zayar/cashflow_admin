# GraphQL API (Vite only exposes VITE_* vars)
# Production on Firebase Hosting (recommended): use Hosting rewrite proxy
VITE_GRAPHQL_URI=/query
# Local backend:
# VITE_GRAPHQL_URI=http://localhost:4000/query
