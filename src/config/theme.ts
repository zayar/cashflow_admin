import { ThemeConfig } from "antd";

const theme: ThemeConfig = {
    token: {
        colorPrimary: '#254394',
        borderRadius: 1,
        colorBgContainer: '#ffff',
    }
};
export default theme;